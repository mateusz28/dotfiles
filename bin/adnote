#!/bin/bash
mkdir -p ~/notes/
echo -ne "#Quicknote: "
read
rclone copy notes: ~/notes
NOTES_NAME=$(date +notes_%Y-%m-%d_${HOSTNAME}.md)
echo "#$REPLY" >> $HOME/notes/$NOTES_NAME
echo "$@" >> $HOME/notes/$NOTES_NAME
rclone copy  ~/notes/ notes: &
