#!/bin/bash
fag() {
  local file
  local line

  read -r file line <<<"$(ag --hidden -u --nobreak --noheading "$@" | fzf -0 -1 | awk -F: '{print $1, $2}')"

  if [[ -n $file ]]
  then
    nvim $file +$line
  fi

}
fag "$@"
